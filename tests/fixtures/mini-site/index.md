# Welcome

This is the tiny fixture site the framework's own build/serve/test entry
points use by default (M1 corrective deliverable 5). It carries no
site-specific branding -- a real site supplies its own content directory
and config instead.
